{{- if .Values.sandboxWorker.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "blackglass.fullname" . }}-sandbox-worker
  labels:
    {{- include "blackglass.labels" . | nindent 4 }}
    app.kubernetes.io/component: sandbox-worker
spec:
  replicas: {{ .Values.sandboxWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "blackglass.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: sandbox-worker
  template:
    metadata:
      labels:
        {{- include "blackglass.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: sandbox-worker
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- if not .Values.secrets.existingSecret }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- end }}
        {{- with .Values.sandboxWorker.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "blackglass.serviceAccountName" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: sandbox-worker
          image: {{ include "blackglass.workerImage" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{ toYaml .Values.sandboxWorker.command | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "blackglass.fullname" . }}-config
            - secretRef:
                name: {{ include "blackglass.envSecretName" . }}
          {{- with .Values.sandboxWorker.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.sandboxWorker.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          # SSHing into freshly-provisioned sandbox Droplets churns
          # known_hosts entries; /tmp must be writable for ssh2.
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.sandboxWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.sandboxWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.sandboxWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
