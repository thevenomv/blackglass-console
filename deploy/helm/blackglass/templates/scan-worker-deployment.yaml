{{- if .Values.scanWorker.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "blackglass.fullname" . }}-scan-worker
  labels:
    {{- include "blackglass.labels" . | nindent 4 }}
    app.kubernetes.io/component: scan-worker
spec:
  replicas: {{ .Values.scanWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "blackglass.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: scan-worker
  template:
    metadata:
      labels:
        {{- include "blackglass.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: scan-worker
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- if not .Values.secrets.existingSecret }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- end }}
        {{- with .Values.scanWorker.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "blackglass.serviceAccountName" . }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: scan-worker
          image: {{ include "blackglass.workerImage" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{ toYaml .Values.scanWorker.command | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "blackglass.fullname" . }}-config
            - secretRef:
                name: {{ include "blackglass.envSecretName" . }}
          {{- with .Values.scanWorker.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.scanWorker.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          # Workers shell out via ssh2; need /tmp writable for occasional
          # known_hosts churn even though we never persist it.
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.scanWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scanWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scanWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
