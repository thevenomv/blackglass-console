If curl from GitHub is not an option, open the Droplet Console for 206.189.114.207 (NOT 167.99.59.55).

Type these commands one at a time (or copy each block separately, not the whole file from Word):

1) See how hbbs is started:
   systemctl cat hbbs.service

2) If ExecStart is exactly /opt/rustdesk-server/amd64/hbbs with no extra args, run:

mkdir -p /etc/systemd/system/hbbs.service.d
cat > /etc/systemd/system/hbbs.service.d/10-relay.conf << 'EOF'
[Service]
ExecStart=
ExecStart=/opt/rustdesk-server/amd64/hbbs -r 206.189.114.207
EOF
systemctl daemon-reload
systemctl restart hbbs.service

3) If step (1) shows extra flags after hbbs (e.g. -k ...), add ONLY " -r 206.189.114.207" to the end of that same line inside the drop-in ExecStart= line.

4) Add your SSH key (replace with your id_ed25519.pub line):
   mkdir -p ~/.ssh && chmod 700 ~/.ssh
   echo 'PASTE_YOUR_PUBLIC_KEY_ONE_LINE' >> ~/.ssh/authorized_keys
   chmod 600 ~/.ssh/authorized_keys
